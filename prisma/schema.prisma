generator client {
  provider = "prisma-client"
  output   = "./client"
  moduleFormat = "esm"
  runtime = "edge-light"
}

datasource db {
  provider     = "postgresql"
  relationMode = "prisma"
}

model views {
  slug  String @id @db.VarChar(128)
  count BigInt @default(1)
}
